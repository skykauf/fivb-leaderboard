# Test pipeline config: limits for a quick ETL + dbt run (~3–5 min).
# Usage:
#   set -a && source env.test.example && set +a
#   python -m etl.load_raw
#   dbt build
# Or run: ./scripts/run_test_pipeline.sh
# Requires .env (DATABASE_URL) to be set separately.
# ETL_PARALLEL=0 for sequential run (set to 1 to test parallel).

TRUNCATE_RAW=1
LIMIT_TOURNAMENTS=15
LIMIT_MATCHES_PER_TOURNAMENT=150
LIMIT_RESULTS_PER_TOURNAMENT=100
ETL_PARALLEL=0
# ETL_MAX_WORKERS=4
